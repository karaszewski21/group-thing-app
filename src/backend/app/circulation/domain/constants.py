"""Pure circulation-domain constants: the emission account code, the
default lend duration, and the flat per-transaction posted amount."""

from __future__ import annotations

from decimal import Decimal

_EMISSION_ACCOUNT_CODE = "900-100"
_DEFAULT_LEND_DAYS = 14
_POSTED_AMOUNT = Decimal("1")
